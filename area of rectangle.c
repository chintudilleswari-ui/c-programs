//finding the area of rectangle
#include<stdio.h>
int main()
{
	float len,wid,area;
	printf("Enter length& width of reactangle:");
	scanf("%f%f",&len,&wid);
	area+len*wid;
	printf("Area of rectangle=%.3f\n",area);
	return 0;
}
